generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model fcmToken{
  userID_Domain   String @db.VarChar(255)    @id
  token String @db.VarChar(255)
  platform     Platform
}

enum Platform {
  IOS
  Android
}
